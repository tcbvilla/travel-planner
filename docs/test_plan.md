# 测试计划与验收标准

## 覆盖范围
- CSV 导入与校验：字段/编码/重复/缺失处理
- 计算正确性：增量、出勤判定、分组统计
- 奖惩与三消：规则匹配与自动合成
- 导出图片：内容与样式正确
- 权限与编辑边界：管理员可编辑、成员只读

## 关键验收
- 同一批 CSV 导入多次，结果一致；
- 任意成员缺失/分组变更处理符合“以起始快照为准”；
- 三消累计与奖金图与记录一致；
- 前后端构建通过，主要浏览器与移动端适配。

